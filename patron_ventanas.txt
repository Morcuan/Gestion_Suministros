class VentanaNuevoContrato(QWidget):
    def __init__(self, conn, cursor):
        super().__init__()
        self.conn = conn
        self.cursor = cursor

Y desde main_window:

ventana = VentanaNuevoContrato(self.conn, self.cursor)
self.cargar_modulo(ventana, "Nuevo contrato")
